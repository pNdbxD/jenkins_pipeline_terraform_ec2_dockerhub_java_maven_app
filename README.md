In this project we create an AWS EC2 instance wtih docker using terraform, then
deploy a java maven app with db from dockerhub.
It uses shared lib jenkins_pipeline_terraform_ec2_dockerhub_java_maven_app_shared_groovy_lib in jenkins file


build artefact
push to dockerhub
run pipeline


